<?xml version="1.0" encoding="utf-8"?>
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <!-- Only include policies we actually need -->
        <force-lock />
        <disable-keyguard-features />
        <!-- Block uninstallation requires device owner privileges, not just device admin -->
    </uses-policies>
</device-admin>
